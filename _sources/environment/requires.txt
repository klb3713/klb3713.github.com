==============
依赖的软件列表
==============
In MongoDB, a **document** is roughly equivalent to a **row** in an RDBMS. When
working with relational databases, rows are stored in **tables**, which have a
strict **schema** that the rows follow. MongoDB stores documents in
**collections** rather than tables - the principle difference is that no schema
is enforced at a database level.

MongoDB
=======
MongoEngine allows you to define schemata for documents as this helps to reduce
coding errors, and allows for utility methods to be defined on fields which may
be present.

Python
======
版本: >=2.6

Setuptools
==========
.. code-block:: console

    $ sudo apt-get install python-setuptools

pymongo
=======
版本:pymongo 2.1.1

.. code-block:: console

    $ sudo apt-get install python-dev
    $ sudo easy_install pymongo

mongoengine
===========
版本:mongoengine 0.6.3

.. code-block:: console

    $ sudo easy_install -U mongoengine

Scrapy
======
Scrapy 暂时还没有用到

.. code-block:: console

    $ sudo easy_install -U Scrapy 
    
RockMongo
=========
RockMongo是一个PHP5写的MongoDB管理工具.

1.使用如下命令安装Apache、PHP

.. code-block:: console

    $ sudo apt-get install apache2 php5 php5-dev php5-cli libapache2-mod-php5
    
2.使用如下命令下载RockMongo

.. code-block:: console

    $ cd /var/www
    $ wget http://rock-php.googlecode.com/files/rockmongo-v1.1.0.zip
    $ unzip rockmongo-v1.1.0.zip
    
3.在https://github.com/mongodb/mongo-php-driver 下载php_mongo module后，解压并使用如下命令编译安装

.. code-block:: console

    $ phpize
    $ ./configure
    $ sudo make install
    
4.在安装完成后，使用如下命令可检查是否安装成功

.. code-block:: console

    $ php -i | grep extension_dir
    extension_dir => /usr/lib/php5/20060613+lfs => /usr/lib/php5/20060613+lfs
    
5.在/etc/php5/apache2/php.ini文件中增加如下内容

.. code-block:: console

    extension=mongo.so
    
重新启动Apache服务后,登录浏览器http:localhost/rockmongo,使用用户名及密码为admin登录

Django
======
数据库使用 MySql, 首先安装mysql-python

.. code-block:: console

    $ sudo apt-get install python-mysqldb
    


